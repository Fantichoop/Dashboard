БОЛЕЕ ПОДРОБНАЯ ИНФА, ЧТО ИМЕННО НУЖНО ЕЩЁ СДЕЛАТЬ:

1) В базу данных сейчас сохраняются данные более-менее нормально, НО там не указывается когда именно они были добавлена. Поэтому нужно сделать так, чтобы в 
этой функции:

def save_to_db(detailed, wind, humidity, temperature, rain, heat_index, clouds):
    weather_data = WeatherData(
        detailed_status=detailed,
        wind=str(wind),
        humidity=humidity,
        temperature=temperature,
        rain=str(rain),
        heat_index=heat_index,
        clouds=clouds
    )
    db.session.add(weather_data)
    db.session.commit()

была добавлена такая состовляющая.
Пример как должно быть: есть данные(detailed, wind, humidity, temperature, rain, heat_index, clouds) - после них должны идти данные со временем(2023,12,03,14)(год, месяц, день, час)

2)ПОСЛЕ ЭТОГО можно уже преобразовывать наш сайт в более приемлимый вид.

В ОСНОВНОМ ВСЕ НИЖНИЕ ПРЕОБРАЗОВАНИЯ ДЕЛАЮТСЯ В ФАЙЛАХ .HTML

Слева на главном сайте будет всё та же инфа о погоде в Томске в ДАННЫЙ момент времени.

Теперь на главной странице по центру сверху будет висеть табличка "Погода в Томске". В самом центре сайта будет поле ввода даты - год, месяц, день. Без ввода часа, так как это будет слишком муторно. Чуть ниже поля ввода должна быть иконка с надписью "Искать", которая перенесёт пользователя, если тот нажал на неё, перед этим введя точную дату, на другую страницу, где будет отображаться вся доступная информация об этом дне - там всякие графики, чего больше, чего меньше в процентном соотношении. Если же пользователь ввёл дату, которой нет в базе данных, то там же на главной странице вылезет уведомление "Информации о данном периоде времени нет". 

3)Когда всё вышеперечисленное будет сделано, нужно сделать новый поиск. Он будет в самом низу главной страницы. Там будет сразу два ввода, где нужно ввести ОТ какой даты искать ДО какой искать инфу, и уже если есть данные об этом промежутке времени нас перенесёт на страницу, где будет показана вся инфа, как и во втором пункте.

PS: 
Кстати, переход к новым страницам делается хрен знает как, но вот сами новые страницы делаются в новом файле .HTML. 
К примеру, главная страница у нас в файле index.html.
И знайте, что сам файл о новых страницах будет один, а не дофига, просто в самом файле будет код, где описывается, что он создаётся автоматически при вызове определённой даты, но когда пользователь выходит, удаляется, точнее просто нигде не сохраняется.

4) А ещё нужно UML обновать, про неё совсем забыли.

