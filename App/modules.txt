Эти модули нужно установить:

pip install

pip install

pip install json

pip install flask

pip install flask pandas

pip install pyowm

pip install Flask-Migrate

pip install matplotlib

pip install sqlalchemy

pip install numpy

pip install

pip install

pip install

pip install

pip install